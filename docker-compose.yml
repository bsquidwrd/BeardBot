version: '3'
services:

  bot:
    image: bsquidwrd/beardbot:latest
    restart: unless-stopped
    command: ["python", "bot.py"]
    networks:
      - default
      - backend
    depends_on:
      - db

  streamlabs:
    image: bsquidwrd/beardbot:latest
    restart: unless-stopped
    command: ["python", "streamlabs.py"]
    networks:
      - default
      - backend
    depends_on:
      - db

  db:
    image: mysql:5.7.26
    restart: unless-stopped
    volumes:
      - db:/var/lib/mysql
    networks:
      backend:
        aliases:
          - beardbot_db

networks:
  backend:
    internal: true

volumes:
  db:
